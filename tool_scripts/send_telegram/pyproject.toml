[project]
name = "send-telegram"
version = "0.1.0"
description = "Send messages to Telegram via bot API"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
send_telegram = "send_message:main"
